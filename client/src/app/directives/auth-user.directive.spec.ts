import { AuthUserDirective } from './auth-user.directive';

describe('AuthUserDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthUserDirective();
    expect(directive).toBeTruthy();
  });
});
